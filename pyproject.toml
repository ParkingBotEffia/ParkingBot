[project]
name = "parkingbot"
version = "0.1.0"
description = "A Python side project."
requires-python = ">=3.9"
readme = "README.md"

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
